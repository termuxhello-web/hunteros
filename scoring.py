def score_app(app_pid):
    # Placeholder: evaluate syscall, network, file access
    score = 0
    # If app does suspicious actions, increase score
    return score

def is_dangerous(score):
    return score > 50  # Threshold
